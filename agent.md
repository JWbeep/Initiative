# AGENTS.md
> **새 프로젝트를 시작할 때**: `## 1. Context` 섹션만 채우세요.
> 나머지 섹션은 어떤 프로젝트에도 공통으로 적용되는 규칙입니다.

---

## 1. 🗂 Context (프로젝트 정보)

| 항목 | 내용 |
|------|------|
| **프로젝트명** | 🍜 롱단쓰 AI 블로그 생성기 (Blog_generator) |
| **한 줄 목표** | 기존 블로그의 글쓰기 스타일을 분석하여 새로운 주제로 블로그 포스팅 초안 자동 생성 |
| **기술 스택** | Python, Streamlit, Google Gemini API (Flash), BeautifulSoup4 |
| **실행 방법** | `streamlit run app.py` |
| **배포 환경** | 로컬 환경 또는 Streamlit Cloud |
| **주요 입력** | 블로그 주제, 핵심 키워드 및 반복 횟수, 메뉴/가격(선택), 반드시 포함할 내용(선택) |
| **주요 출력** | 롱단쓰 스타일이 반영된 블로그 초안 (마크다운 굵게 표시 제외) |

---

## 2. 🔐 환경 변수 관리 규칙

- API 키와 비밀값은 반드시 `.env` 파일에만 저장한다.
- 코드 내에서는 `python-dotenv`의 `load_dotenv()`로만 불러온다.
- .env 파일은 절대 Git에 커밋하지 않는다. (`.gitignore` 등록 확인)
- GitHub에는 `.env.example`만 올린다 — 실제 값 없이 키 이름만 기재.
- **[실수 방지]** 에이전트는 푸시 전 `.env.example`에 실제 API 키(예: `your_key_here` 이외의 값)가 포함되어 있는지 반드시 검사한다.

```python
# 올바른 사용 예
from dotenv import load_dotenv
import os

load_dotenv()
API_KEY = os.getenv("MY_API_KEY")
```

---

## 3. 📐 코딩 컨벤션

| 대상 | 규칙 |
|------|------|
| 함수 / 변수 | `snake_case` |
| 클래스 | `PascalCase` |
| 상수 | `UPPER_SNAKE_CASE` |
| 파일 1개 | 역할 1개 (기능별로 모듈 분리) |
| 주석 / 변수명 | 영어 작성 |
| 사용자 출력 메시지 | 한국어 작성 |
| API 호출 | 반드시 `try-except`로 예외 처리 |

---

## 4. 🔄 작업 흐름 (Workflow)

```
1단계  요구사항 파악   — 무엇을 만들지 구체적으로 정의
2단계  설계           — 어떤 파일/함수를 추가·수정할지 계획
3단계  구현           — 코드 작성
4단계  로컬 테스트    — 직접 실행하여 동작 확인
5단계  Git 커밋       — 의미 있는 단위로 커밋
6단계  보안 검사       — .env.example 등에 실제 키가 포함됐는지 최종 확인
7단계  푸시 & 배포    — GitHub 푸시 → 배포 환경 확인
```

### 커밋 메시지 형식
```
feat:     새 기능 추가
fix:      버그 수정
refactor: 코드 개선 (기능 변경 없음)
docs:     문서 수정
chore:    설정, 패키지 등 잡무
```

---

## 5. 🧰 에이전트 권한

### 허용
- 프로젝트 폴더 내 파일 읽기 / 생성 / 수정
- 터미널: 가상환경 생성·활성화, `pip install`, 실행 명령
- 브라우저: 공식 문서, 레퍼런스 검색
- Git: `add`, `commit`, `push`

### 금지
- `.env`의 실제 키 값을 화면·로그에 출력하는 행위
- 프로젝트 폴더 **외부** 파일 수정
- 사용자 승인 없이 새 패키지 설치
- 기존 데이터 파일을 확인 없이 덮어쓰기

---

## 6. ✅ 완료 기준 (Definition of Done)

작업이 끝났다고 말하려면 아래 항목을 모두 통과해야 합니다.

- [ ] 기능이 로컬에서 정상 동작한다
- [ ] `.env` 관련 민감 정보가 코드/커밋에 노출되지 않는다
- [ ] `.env.example`에 실제 API 키가 아닌 템플릿 값만 들어있음을 확인했다
- [ ] `requirements.txt`가 최신 상태다
- [ ] 의미 있는 커밋 메시지로 Git 푸시가 완료됐다

---

## 7. 🎯 특정 생성 규칙 (Project Specific Rules)

이 프로젝트는 블로거 '롱단쓰'의 스타일을 재현하므로 다음 규칙을 반드시 준수한다.

- **마크다운 굵게 표시(`**`) 절대 금지**: 소제목이나 강조에도 별표 기호를 쓰지 않고 텍스트로만 작성한다.
- **필수 구성 요소**: 글 초반부에 반드시 차량 주차(🚗), 영업시간(⏰), 전화번호(☎️) 정보를 포함하는 레이아웃을 작성한다.
- **문체 및 어미**: 친근한 구어체와 적절한 이모티콘을 사용한다.
- **데이터 활용**: `blog_generator/data/processed` 폴더의 기존 글들을 예시(Few-shot)로 참고하여 매번 다양한 말투를 반영한다.
