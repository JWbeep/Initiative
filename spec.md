# 📋 Project Specification (작업 명세서)

이 문서는 프로젝트의 세부 설정값과 동작 규칙을 정의합니다. 사용자(User)는 이 문서의 수치를 수정하여 프로젝트의 기능을 제어하고, 에이전트(AI)는 이 명세서를 최우선 지침으로 삼아 코드를 작성합니다.

---

## 1. UI 설정 (app.py 관련)

| 항목 | 현재 설정값 | 설명 |
|------|------------|------|
| **최대 키워드 개수** | 4개 | 사용자로부터 입력받을 핵심 키워드 창의 개수 |
| **최대 메뉴 개수** | 5개 | 사용자로부터 입력받을 메뉴/가격 창의 개수 |
| **최대 필수 문구 개수** | 3개 | 반드시 포함해야 하는 문장 입력창의 개수 |
| **결과창 높이** | 600px | 생성된 블로그 결과물이 출력되는 영역의 높이 |

---

## 2. 블로그 생성 로직 (generator.py 관련)

| 항목 | 현재 설정값 | 설명 |
|------|------------|------|
| **학습 데이터 활용 개수** | 4개 | 말투 학습을 위해 무작위로 선택할 예시 글의 개수 |
| **예시 데이터 최대 길이** | 2000자 | 각 예시 글에서 AI에게 학습시킬 최대 글자 수 |
| **마크다운 굵게(`**`)** | **사용 금지** | 결과물에서 굵게 표시 기호를 절대 사용하지 않음 |

---

## 3. 필수 레이아웃 및 포맷 (네이버 지도 연동)

AI는 블로그 글 생성 시 아래의 구조를 반드시 지켜야 합니다.

- **1순위**: `네이버 지도 URL` 기반 자동 추출값 적용
- **2순위**: (추출 실패 시) 기존 로직대로 프롬프트에 지시하여 자연스럽게 지어서 채움. 영업시간과 전화번호는 1순위에서 정보가 없을 경우 '정보없음'으로 표시

### 🚗 주차 정보
- 글 초반(위치 소개 근처)에 위치해야 함
- 데이터가 없을 경우 "정보 없음" 또는 "인근 공영주차장 이용" 등으로 자연스럽게 채움

### ⏰ 영업시간
- 요일별 또는 시간대별 정보를 상세히 나열

### ☎️ 전화번호
- 매장 연락처 정보를 포함

---

## 4. 말투 및 스타일 가이드
- **어미**: 친근한 구어체 사용
- **이모지**: 각 문맥에 맞는 이모지를 적재적소에 배치
- **인사**: 항상 "안녕하세요, 롱단쓰입니다"로 시작

---

## 🔄 협업 프로세스 가이드
1. 사용자가 이 파일(`spec.md`)의 수치나 규칙을 직접 수정한다.
2. AI에게 **"spec.md의 변경 사항을 코드에 반영해줘"**라고 명령한다.
3. AI는 이 파일을 읽고 `app.py` 또는 `generator.py` 등의 소스 코드를 그에 맞춰 업데이트한다.
